<Window x:Class="Projet.ParametresWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Paramètres" Height="250" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Nom utilisateur:"/>
            <TextBox x:Name="NomUtilisateurBox" Margin="0,5"/>
            <TextBlock Text="Taille police:"/>
            <TextBox x:Name="TaillePoliceBox" Margin="0,5"/>
            <CheckBox x:Name="ModeSombreCheck" Content="Mode sombre" Margin="0,5"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Annuler" Click="Annuler_Click" Margin="0,0,10,0"/>
            <Button Content="Enregistrer" Click="Enregistrer_Click"/>
        </StackPanel>
    </Grid>
</Window>
